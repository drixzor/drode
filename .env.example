# Drode OAuth2 Credentials
# Copy this file to .env and fill in your values.
# The app reads these at runtime via std::env — load them however you prefer
# (shell export, direnv, dotenv, or launch config).

# ── GitHub ────────────────────────────────────────────
# Create an OAuth App at: https://github.com/settings/developers
#   → "New OAuth App"
#   → Homepage URL: http://localhost:1420
#   → Authorization callback URL: http://127.0.0.1:17391/callback
DRODE_GITHUB_CLIENT_ID=
DRODE_GITHUB_CLIENT_ID_SECRET=

# ── Supabase ──────────────────────────────────────────
# Create an OAuth App in your Supabase org settings:
#   → https://supabase.com/dashboard/org/_/apps
#   → "New OAuth App"
#   → Redirect URL: http://127.0.0.1:17391/callback
DRODE_SUPABASE_CLIENT_ID=
DRODE_SUPABASE_CLIENT_ID_SECRET=

# ── Vercel ────────────────────────────────────────────
# Create an OAuth App at: https://vercel.com/account/tokens
#   → "OAuth Apps" → "Create"
#   → Redirect URL: http://127.0.0.1:17391/callback
DRODE_VERCEL_CLIENT_ID=
DRODE_VERCEL_CLIENT_ID_SECRET=
